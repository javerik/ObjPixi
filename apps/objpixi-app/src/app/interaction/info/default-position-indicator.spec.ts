import { DefaultPositionIndicator } from './default-position-indicator';

describe('DefaultPositionIndicator', () => {
  it('should create an instance', () => {
    expect(new DefaultPositionIndicator()).toBeTruthy();
  });
});
