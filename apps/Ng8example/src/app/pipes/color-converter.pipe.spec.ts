import { ColorConverterPipe } from './color-converter.pipe';

describe('ColorConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
